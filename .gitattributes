# Enforce LF line endings for all code files (recommended cross-platform practice)
*.js text eol=lf
*.json text eol=lf
*.html text eol=lf
*.css text eol=lf
*.php text eol=lf
*.sql text eol=lf
*.sh text eol=lf

# Binary files should be treated as-is (no line ending conversion)
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.zip binary
*.exe binary
*.dll binary

# Node modules (optional, usually not version controlled)
node_modules/* -text

# Disable automatic line ending normalization (optional strict)
* text=auto
